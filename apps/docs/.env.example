# Referenced by NUXT_PUBLIC_DOCS_URL in apps/frontend/.env.
PORT=5173

# Must match the frontend's PORT (apps/frontend/.env).
# Baked in at build time; Docker overrides it with '/' — see apps/docs/Dockerfile.
DASHBOARD_URL=http://localhost:3200
